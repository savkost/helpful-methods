import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '02-Helpful-Methods-Every-Project',
  webDir: 'www'
};

export default config;
